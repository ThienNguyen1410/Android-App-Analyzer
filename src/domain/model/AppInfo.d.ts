export interface AppInfo {
  title: string;
  appId: string;
  recentChanges: string;
  updateTimestamp: number;
  updateDate: string;
  updateTime: string;
  version: string;
  androidVersion: string;
  developer: string;
}
